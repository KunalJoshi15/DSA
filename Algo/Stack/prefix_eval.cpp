#include<bits/stdc++.h>

using namespace std;
// The approach that will be used for solving this particular question is that we will be converting the prefix expression into the infix one and then 
// we can simply 

// + * 10 2 3
int main()
{
	// The prefix evaluation follows the same approach as the postfix evaluation here the traversal will be right to left rather than left to right
	// When is one opertor excountered then we will simply pop the top two elements and then perform the operation which has been specified.
}