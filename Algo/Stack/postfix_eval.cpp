#include<bits/stdc++.h>

using namespace std;
// whenever we see an operator then we simply pop the top two elements from the stack and then perform the operations to that 
// and then push the result onto the stack.

int main()
{

}