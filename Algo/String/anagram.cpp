#include<bits/stdc++.h>

using namespace std;

int main()
{
    // Two strings are to be said the anagrams of each other if one is the permutation of other.
    // In the naive approach we use the sorting technique while better approach will be using the hashmap.
}