#include<bits/stdc++.h>

using namespace std;

int main()
{
    int arr[] = {1,4,3};
    // There is one value missing in the array from 1 to n
    // XOR is performed for all the numbers from 1 to n+1 and then XOR with the elements of the array is performed.
    // If the all the values present in the array will get the XOR as 0 while all the other numbers will have the duplicate therefore the value which we will get after the operation will be the missing number form the array.
}