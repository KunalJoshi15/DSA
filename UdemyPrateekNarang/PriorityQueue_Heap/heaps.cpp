#include<bits/stdc++.h>

using namespace std;
// Heap is the data structure which can be used by us as per the requirement. The priority queue is the heap data structure.
// It is basically an almost complete binary tree.
// Filling is always in left to right order.
// All the operations that are being performed in the heap data structure. This will take the O(log n) time complexity.
// The max element in case of the heap data structure can be found in the O(1) complexity as the root element of the max heap is the largest element.
int main()
{

}