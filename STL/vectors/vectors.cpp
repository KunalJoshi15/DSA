#include<bits/stdc++.h>
using namespace std;

int main()
{
    vector<int> a{1,2,3,4,5};
    vector<int> b(3,10);
    // There willl be the three values having the value as 10
    /*
    pop_back();
    front();
    back();
    insert(iterator,value)
    erase function for it we need to pass an iterator.
    empty function is used to check whether the vector returned is empty or not.
    */
    return 0;
}