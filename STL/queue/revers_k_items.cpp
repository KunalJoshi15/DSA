#include<bits/stdc++.h>
using namespace std;

int main()
{
    /*
        Reversing the first k items follows the basic approach. 
        We will create a stack and will push the first three items into it and then pop these items from 
        the stack and insert then into queue. These will be in reverse order 
        then we will simply remove the element from the queue and will insert back onto the queue.
    */ 
}