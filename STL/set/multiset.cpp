#include<bits/stdc++.h>

using namespace std;

int main()
{
    // multiset in C++ also use the same concept as the set the red black tree here as well is the 
    // Underlying structure.
    // We can have multiple instances of the key.
    return 0;
}