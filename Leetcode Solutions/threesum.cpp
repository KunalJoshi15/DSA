#include<bits/stdc++.h>

using namespace std;

vector<int> findSol(vector<int> &arr,int n){
	unordered_set<int>
}

int main(){
	vector<int> arr = {-1,0,1,2,-1,-4};
	int n = arr.size();
	vector<int> solution = findSol(arr,n);
}