#include<bits/stdc++.h>

using namespace std;
// In case  of the naive approach the non zero elements are 
// replaced with the zero elements.
//  each non zero element that we have encountered is swapped with the first zero element which has been encountered.