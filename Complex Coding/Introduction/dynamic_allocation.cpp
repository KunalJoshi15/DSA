#include<bits/stdc++.h>

using namespace std;

int main(){
    int *arr = new int[10];
    delete[] arr;
    // This will delete the memory allocated to the array.
}
// The dynamically allocated memory needs to be always deleted.